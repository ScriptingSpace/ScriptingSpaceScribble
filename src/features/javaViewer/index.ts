// Barrel export for the Java viewer feature. The module's side-effect
// (plugin registration) fires on import — see JavaViewerFeature.tsx's
// registerScribblePlugin call.
export * from './JavaViewerFeature';
