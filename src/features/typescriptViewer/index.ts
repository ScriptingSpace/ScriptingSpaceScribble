// Barrel export for the TypeScript/JavaScript viewer feature. The module's
// side-effect (plugin registration) fires on import — see
// TypeScriptViewerFeature.tsx's registerScribblePlugin call.
export * from './TypeScriptViewerFeature';
