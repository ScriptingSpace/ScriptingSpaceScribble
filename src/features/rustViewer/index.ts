// Barrel export for the Rust viewer feature. The module's side-effect
// (plugin registration) fires on import — see RustViewerFeature.tsx's
// registerScribblePlugin call.
export * from './RustViewerFeature';
