// Barrel export for the Python viewer feature. The module's side-effect
// (plugin registration) fires on import — see PythonViewerFeature.tsx's
// registerScribblePlugin call.
export * from './PythonViewerFeature';
